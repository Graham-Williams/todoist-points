# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/
build/
dist/

# secrets — NEVER commit these (catch-all; only the placeholder example is tracked)
.env*
!.env.example

# database / local data — NEVER commit (contains personal task + points data)
*.db
*.sqlite
*.sqlite3
data/
*.db-journal

# misc
.DS_Store
*.log
npm-debug.log*
.vercel
coverage/
